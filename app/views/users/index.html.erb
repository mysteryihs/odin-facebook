<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<% @users.each do |user| %>
	<%= user.email %> <%= link_to 'Send Friend Request', create_friend_request_path(user.id), :method => :post if user != current_user %> <br>
<% end %>